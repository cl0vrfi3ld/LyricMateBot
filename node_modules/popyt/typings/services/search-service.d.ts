import YouTube, { Video, Channel, Playlist } from '..';
/**
 * @ignore
 */
export declare class SearchService {
    static search(youtube: YouTube, types: (typeof Video | typeof Channel | typeof Playlist)[], searchTerm: string, maxResults?: number, pageToken?: string): Promise<{
        results: (Video | Channel | Playlist)[];
        prevPageToken: string;
        nextPageToken: string;
    }>;
}
